{
  "name": "@nexora/skills",
  "version": "0.5.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "lint": "find src test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test test/*.test.js",
    "build": "find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "independence": "! grep -R -n -E 'associacao-bolepix|backend/src|express|mongoose|mongodb|pm2' src test"
  }
}
