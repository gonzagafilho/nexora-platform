const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");
test("source has no forbidden runtime dependencies",()=>{const forbidden=new RegExp(["asso"+"ciacao-bo"+"lepix","back"+"end/src","exp"+"ress","mong"+"oose","mong"+"odb","p"+"m2"].join("|"),"i");function walk(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(dir,e.name)):[path.join(dir,e.name)])}const text=walk(path.join(__dirname,"../src")).map(f=>fs.readFileSync(f,"utf8")).join("\n");assert.equal(forbidden.test(text),false)});
