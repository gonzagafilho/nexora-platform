function createAgentEventEmitter(options = {}) { const handlers = new Map(); const history = []; const limit = options.historyLimit ?? 1000; return { emit(eventName, payload = {}) { const record = { eventName, payload, timestamp: new Date().toISOString() }; history.push(record); if (history.length > limit) history.shift(); for (const fn of handlers.get(eventName) || []) fn(payload, record); return record; }, on(name, fn) { if (!handlers.has(name)) handlers.set(name, new Set()); handlers.get(name).add(fn); return () => this.off(name, fn); }, off(name, fn) { return handlers.get(name)?.delete(fn) || false; }, listEvents() { return [...handlers.keys()]; }, getHistory() { return history.slice(); } }; }
module.exports = { createAgentEventEmitter };
